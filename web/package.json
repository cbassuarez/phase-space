{
  "name": "phase-space",
  "version": "1.0.6",
  "private": true,
  "scripts": {
    "build:wasm": "WASM_PACK=$(command -v wasm-pack || true); if [ -z \"$WASM_PACK\" ] && [ -x ../target/wasm-pack/bin/wasm-pack ]; then WASM_PACK=../target/wasm-pack/bin/wasm-pack; fi; if [ -z \"$WASM_PACK\" ]; then echo 'wasm-pack not found; installing locally with cargo'; cargo install wasm-pack --locked --version 0.13.1 --root ../target/wasm-pack; WASM_PACK=../target/wasm-pack/bin/wasm-pack; fi; \"$WASM_PACK\" build ../phasewasm --target web --out-dir ../web/src/wasm --out-name phasewasm",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.2",
    "@semantic-release/release-notes-generator": "^14.1.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@types/react-router-dom": "^5.3.3",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "semantic-release": "^25.0.2",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "@react-three/fiber": "^8.16.1",
    "clsx": "^2.1.1",
    "framer-motion": "^11.18.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.9.6",
    "three": "^0.181.2"
  }
}
